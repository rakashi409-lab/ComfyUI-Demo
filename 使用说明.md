# ComfyUI 演示程序

一个单文件的 ComfyUI 界面模拟程序，用于工作流搭建演示。无需安装任何软件或模型。

## 使用方法

双击 `ComfyUI.html`，用浏览器（推荐 Chrome / Edge）打开即可。

## 基本操作

| 操作 | 说明 |
|---|---|
| 双击 / 右键空白画布 | 弹出节点搜索，添加节点 |
| 左键拖空白处 | 平移画布 |
| 鼠标中键拖动 | 任意位置平移画布（包括节点上） |
| 滚轮 | 缩放画布 |
| 拖动节点标题栏 | 移动节点 |
| 拖动节点右下角三角 | 调整节点大小 |
| 从输出端口拖线到输入端口 | 连接节点（颜色类型匹配才能连） |
| 从已连接的输入端口拖动 | 断开并接管该连线 |
| Delete | 删除选中节点 |
| Ctrl+Z / Ctrl+Shift+Z | 撤销 / 重做 |

## 左侧边栏

- **第二个图标（节点库）**：列出全部可用节点，点击添加到视野中央，或拖拽到画布指定位置。顶部蓝色按钮可一键加载预设工作流 F.2-Klein9b编辑3（19 个节点、连线和参数完整还原）。
- **底部时钟图标（生成历史）**：演示核心功能，见下文。

## 生成历史（演示用）

点左侧边栏底部的时钟图标（或按 Ctrl+Shift+H）打开面板：

1. 演示前，把预先生成好的成品图按顺序上传（可多选、可拖入）
2. 点顶部"执行"按钮，工作流会按真实的依赖顺序模拟运行（含并行、进度条、采样步进）
3. 执行到"保存图像"节点时，按顺序显示第 1 张、第 2 张……
4. 面板底部可"回退上一步"（多点了一次执行时纠正）或"重置进度"（从第一张重新开始）

## 说明

- 纯前端单文件，无任何网络请求，所有数据仅在浏览器内存中
- 刷新页面会清空画布和已上传的图片
