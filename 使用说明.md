# ComfyUI Demo v2.0.1 使用说明

## 打开程序

在线：https://rakashi409-lab.github.io/ComfyUI-Demo/

离线：解压免安装 ZIP，双击 `ComfyUI.html` 或 `启动演示.bat`。无需安装 Python、Node.js、模型或显卡驱动。

## 背景色板 / 分组标签

- 点击顶部「＋ 分组」或按 G 创建一个背景框，框位于节点和连线下方。
- 点击标题旁的 ✎，或双击标题，打开右侧编辑面板。
- 标签名称可自由编辑，支持中文；标题字号可设为 12–64。
- 颜色可通过连续取色器或六位 HEX 自定义；预设色只是快捷入口，不限制可选颜色。背景透明度为 0–100%。
- 拖动标题移动；拖动右边、底边、右下角改变尺寸。也可输入宽度和高度，最小 120×80，无网格吸附。
- 开启「移动时携带组内节点」后，会带走拖动开始时完整包围的节点。按住 Alt 可临时只移动背景框。
- Delete 删除选中的分组框，保留节点。Ctrl/Cmd+D 复制选中框（仅框）或节点（不复制连线）。分组操作支持撤销/重做。

## 工作流与图片保存

「保存」写入当前浏览器的 IndexedDB；「导出 JSON」下载完整备份文件。状态栏出现「已保存」后可安全刷新。

完整备份包括节点、连线、分组颜色/尺寸/标题、控件值、输入图片、结果图片、演示图片列表及播放位置、工作流名称、画布缩放位置和设置。

「导入 JSON」会先检查整个文件，检查通过后再替换画布。导入错误不会覆盖当前内容。替换后仍可撤销，但长期保留请导出备份。

自动保存只在同一浏览器、同一站点生效。离线文件移动、浏览器清理、隐私模式或更换域名可能使原保存不可访问。存储不可用或空间不足时会明确提示，请使用 JSON 导出。

## 演示执行

1. 在节点库加载五色预设，或自行搭建工作流。
2. 在加载图像节点上传输入图。在左侧底部生成历史面板上传预生成图片，上传顺序就是演示顺序。
3. 点击执行。模型等重型节点按依赖独占执行，轻型节点最多两个并行；采样进度使用已连接的 Flux2Scheduler.steps。
4. 成功完成后，保存图像节点展示本轮成品图，并更新噪声种子。点击节点下的「下载图片」保存原始图片格式。
5. 执行过程中点击停止。停止或出错不会消耗下一张图片，也不会更新种子。

没有保存图像节点时不推进图片播放位置。图片用完后继续展示最后一张。多个保存图像节点展示本轮同一张图片。回退上一步或重置进度可重复演示。

本程序模拟工作流执行，不进行实际模型推理；改变提示词、CFG 或参考图片不会改变预上传的成品图。

## 按钮与快捷键

| 操作 | 功能 |
|---|---|
| 新建 / 工作流标签 × | 清空为新工作流，替换前提示，可撤销 |
| 点击工作流名称 | 重命名 |
| 左侧工作流图标 | 节点/分组统计与备份入口 |
| 左侧模型库 | 查看和修改加载器的模型名称 |
| 左侧图像 | 浏览和下载输入图、输出图和历史图像 |
| 通知 | 当前会话的执行与错误记录 |
| 设置 | 自动保存、0.25–4 倍执行速度、新分组默认颜色 |
| 双击 / 右键空白 | 节点搜索 |
| 左键拖空白 / 中键拖动 | 平移 |
| 滚轮 / F | 缩放 / 适应画布 |
| Ctrl/Cmd+S | 保存到浏览器 |
| Ctrl/Cmd+Shift+S | 导出 JSON |
| Ctrl/Cmd+O | 导入 JSON |
| Ctrl/Cmd+Z | 撤销 |
| Ctrl/Cmd+Shift+Z / Ctrl+Y | 重做 |
| Ctrl/Cmd+D | 复制选中节点或分组 |
| Ctrl/Cmd+Enter | 执行 / 停止 |
| Ctrl+Shift+H | 生成历史 |
| Delete / Backspace | 删除选中对象，输入文字时不触发 |

## 参数范围

尺寸：16–16384；批量：1–64；步数：1–1000；CFG：0–100（0.1 步长）；颜色数值：0–16777215；种子：0–9007199254740991（JavaScript 安全整数范围）。

直接编辑会将越界或无效数字调整到有效值并提示。导入包含越界参数时会拒绝导入，避免悄悄更改工作流。

种子模式：fixed 保持不变；increment 加一；decrement 减一；randomize 使用浏览器随机数。加减到边界时循环到另一端。
